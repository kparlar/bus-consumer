<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kparlar.iett.consumer.repository.MessageLineRepository">
    <update id="upsert" parameterType="com.kparlar.iett.consumer.entity.MessageLineEntity">
        INSERT INTO bus.message_line (message_line_id, message, status, create_date,  update)
        values (#{messageLineId}, #{message}, #{status}, #{createDate}, #{update})
        ON CONFLICT (message_line_id) DO UPDATE
        SET
            status = #{status},
            update = #{update}
        where update <![CDATA[ < ]]> #{update}
    </update>
</mapper>